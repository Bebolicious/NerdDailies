# Copy this file to `.env` and fill in your values.
# Do NOT commit `.env` — it's already in .gitignore by default.

# From your Supabase project: Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Later — when the Netlify function for IGDB is wired up.
# Server-side only, never expose to the client.
# IGDB_CLIENT_ID=
# IGDB_ACCESS_TOKEN=
